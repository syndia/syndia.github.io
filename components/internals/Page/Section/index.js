import styled from "styled-components/primitives";

// @todo Add padding e.g padding: ${props => props.padded ? "96px 32px" : 0 };

const Section = styled.View`
  display: flex;
  flex-direction: column;
  align-items: stretch;
  position: relative;
`;

export default Section;
